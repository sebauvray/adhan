ARG DEBIAN_VERSION=bookworm
FROM debian:${DEBIAN_VERSION}

ARG TZ \
    AUTONOMOUS=false \
    GECKODRIVER_VERSION=v0.34.0

WORKDIR /app

# Packages de base (toujours installés)
RUN apt-get update && apt-get install -y \
    tzdata \
    wget \
    python3 \
    python3-pip \
    cron \
    jq \
    curl \
    && if [ "$AUTONOMOUS" = "true" ]; then apt-get install -y firefox-esr; fi \
    && rm -rf /var/lib/apt/lists/*

# Timezone
RUN ln -snf /usr/share/zoneinfo/$TZ /etc/localtime && echo $TZ > /etc/timezone

# Geckodriver — uniquement si AUTONOMOUS=true
RUN if [ "$AUTONOMOUS" = "true" ]; then \
      ARCH=$(uname -m) && case "$ARCH" in \
        x86_64)  ARCH="linux64" ;; \
        aarch64) ARCH="linux-aarch64" ;; \
        arm*)    ARCH="linux-armhf" ;; \
        *) echo "Architecture non supportée"; exit 1 ;; \
      esac && \
      wget -O geckodriver.tar.gz \
        "https://github.com/mozilla/geckodriver/releases/download/$GECKODRIVER_VERSION/geckodriver-$GECKODRIVER_VERSION-$ARCH.tar.gz" \
      && tar -xzf geckodriver.tar.gz \
      && mv geckodriver /usr/local/bin/ \
      && chmod +x /usr/local/bin/geckodriver \
      && rm geckodriver.tar.gz; \
    fi

# Dépendances Python
COPY docker/requirements.txt .
RUN pip3 install --no-cache-dir --break-system-packages -r requirements.txt \
    && if [ "$AUTONOMOUS" = "true" ]; then \
         pip3 install --no-cache-dir --break-system-packages selenium; \
       fi \
    && rm requirements.txt

# Module DB
COPY ./db /app/db

# Scripts
COPY ./get_time_salat.py /app/get_time_salat.py
COPY ./next_salat.py     /app/next_salat.py
COPY ./load_config.py    /app/load_config.py
COPY ./get_homepods.py   /app/get_homepods.py
COPY ./adhan.sh          /app/adhan.sh
RUN chmod +x /app/adhan.sh

# Providers
COPY ./providers /app/providers

# HomePod.json (migration legacy vers SQLite)
COPY ./HomePod.json /app/HomePod.json

# Cron
COPY ./salat.crontab /etc/cron.d/salat
RUN touch /var/log/cron.log

# Entrypoint
COPY ./startup.sh /opt/startup.sh
RUN chmod +x /opt/startup.sh
CMD ["/opt/startup.sh"]
